// Write a function to return the absolute difference between 2 numbers.
function absoluteDifference(num1, num2) {
    return Math.abs(num1 - num2);
}
const number1 = 10;
const number2 = 7;
const difference = absoluteDifference(number1, number2);
console.log("Absolute difference:", difference);
